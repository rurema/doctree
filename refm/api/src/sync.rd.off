= sync

-> ((<Sync_m>))

: ((<Synchronizer_m>))
  Sync_m の別名です。Sync_m を参照してください。
: ((<Sync>))
  Sync_m をincludeしたクラスです。使い方は Sync_m を参照してください。
: ((<Synchronizer>)) 
  Sync_m をincludeしたクラス Sync の別名。使い方は Sync_m を参照してください。
