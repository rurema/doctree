#@since 1.8.0

= class CSV::Writer < Object

== Class Methods

--- generate(str_or_writable[, fs = ','[, rs = nil]]) { ... }

--- create(str_or_writable[, fs = ','[, rs = nil]])

== Instance Methods

--- <<(row)
--- add_row(row)

--- close

== Private Instance Methods

--- terminate

#@end
