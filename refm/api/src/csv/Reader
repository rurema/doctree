= class CSV::Reader < Object

include Enumerable

CSVの読み込み時のベースとなるクラス

== Class Methods

--- parse(str_or_readable[, fs = ','[, rs = nil]]) { ... }

--- create(str_or_readable[, fs = ','[, rs = nil]])

== Instance Methods

--- each

--- shift

--- close
