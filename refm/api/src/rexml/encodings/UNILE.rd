= reopen REXML::Encoding

== Instance Methods

#@since 1.8.3
--- encode_unile(content)

--- decode_unile(str)
#@end

#@if (version <= "1.8.0")
--- to_unile(content)

--- from_unile(str)
#@end

#@since 1.8.1
= redefine REXML::Encoding

== Instance Methods

--- decode(str)

--- encode(content)
#@end
