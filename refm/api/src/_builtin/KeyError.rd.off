= KeyError

((<ruby 1.9 feature|ruby 1.9 feature/2004-09-22>)):
((<Hash#fetch|Hash/fetch>)) などで key に対応する value がない場合に発生します。

1.8以前は ((<IndexError>)) が発生します。
互換性のため、((<KeyError>)) は ((<IndexError>)) のサブクラスになっています。

== スーパークラス:

* ((<IndexError>))
