= class RDoc::Stats

RDoc のステータスを管理するクラスです。

== Class Methods

--- new -> RDoc::Stats

自身を初期化します。

== Instance Methods

--- print -> ()

自身の持つ情報を標準出力に表示します。

--- num_files -> Integer

解析したファイルの数を返します。

--- num_files=(val)

解析したファイルの数を指定します。

@param val 数値を指定します。

--- num_classes -> Integer

解析したクラスの数を返します。

--- num_classes=(val)

解析したクラスの数を指定します。

@param val 数値を指定します。

--- num_modules -> Integer

解析したモジュールの数を返します。

--- num_modules=(val)

解析したモジュールの数を指定します。

@param val 数値を指定します。

--- num_methods -> Integer

解析したメソッドの数を返します。

--- num_methods=(val)

解析したメソッドの数を指定します。

@param val 数値を指定します。
