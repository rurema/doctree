= module Zlib

[[lib:zlib]]

zlib ライブラリに含まれている雑多な機能を提供するモジュール。
各モジュール関数の詳細は zlib.h を参照して下さい。

== Module Functions

--- zlib_version

zlib ライブラリのバージョンを表す文字列を返します。

--- adler32([string[, adler]])

string の Adler-32 チェックサムを計算し、adler を
更新した値を返します。string が省略された場合は
Adler-32 チェックサムの初期値を返します。adler が
省略された場合は adler に初期値が与えらたものとして
計算します。

--- crc32([string[, crc]])

string の CRC チェックサムを計算し、crc を
更新した値を返します。string が省略された場合は
CRC チェックサムの初期値を返します。crc が
省略された場合は crc に初期値が与えらたものとして
計算します。

--- crc_table

CRC チェックサムの計算に用いるテーブルを配列で返します。

== Constants

--- VERSION

Ruby/zlib のバージョンを表す文字列。

--- ZLIB_VERSION

zlib.h のバージョンを表す文字列。

--- BINARY
--- ASCII
--- UNKNOWN

[[m:Zlib::ZStream#data_type]] の返す、データタイプを表す整数。

--- NO_COMPRESSION
--- BEST_SPEED
--- BEST_COMPRESSION
--- DEFAULT_COMPRESSION

[[m:Zlib::Deflate.new]] や [[m:Zlib::Deflate#deflate]] 等に渡す、
圧縮レベルを表す整数。

--- FILTERED
--- HUFFMAN_ONLY
--- DEFAULT_STRATEGY

[[m:Zlib::Deflate.new]] や [[m:Zlib::Deflate#params]] に渡す、
圧縮方法を表す整数。

--- DEF_MEM_LEVEL
--- MAX_MEM_LEVEL

[[m:Zlib::Deflate.new]] 等に渡す、memory level を表す整数。

--- MAX_WBITS

[[m:Zlib::Deflate.new]] や [[m:Zlib::Inflate.new]] での
windowBits のデフォルト値。

--- NO_FLUSH
--- SYNC_FLUSH
--- FULL_FLUSH
--- FINISH

[[m:Zlib::Deflate#deflate]] 等に渡す、ストリームの出力を
制御するための整数。

--- OS_CODE
--- OS_MSDOS
--- OS_AMIGA
--- OS_VMS
--- OS_UNIX
--- OS_VMCMS
--- OS_ATARI
--- OS_OS2
--- OS_MACOS
--- OS_ZSYSTEM
--- OS_CPM
--- OS_TOPS20
--- OS_WIN32
--- OS_QDOS
--- OS_RISCOS
--- OS_UNKNOWN

[[m:Zlib::GzipFile#os_code]] メソッドの返す値。
