#@if (version >= "1.8.0")
= class OpenSSL::ASN1::Constructive < OpenSSL::ASN1::ASN1Data

== Class Methods
--- new(value, tag, tagging, tag_class)

Constructive オブジェクトを生成します。

== Instance Methods

--- tagging
--- tagging=(tag)

[[c:Symbol]] オブジェクト :IMPLICIT または :EXPLICIT のいずれかをとります。
#@end
