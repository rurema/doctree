#@if (version >= "1.8.0")
= class OpenSSL::X509::Revoked < Object

失効した証明書のクラス。[[c:OpenSSL::X509::CRL]] クラスから使われます。

== Class Methods

--- new
#@todo

== Instance Methods

--- serial
--- serial=(serial)
#@todo

失効した証明書のシリアルです。

--- time
--- time=(time)
#@todo

失効した日時です。[[c:Time]] オブジェクトです。

--- extensions
--- extensions=(extensions)
#@todo

拡張領域です。[[c:OpenSSL::X509::Extension]] の配列です。

--- add_extension(ex)
#@todo

拡張領域に値を加えます。

= class OpenSSL::X509::RevokedError < OpenSSL::OpenSSLError
#@end
