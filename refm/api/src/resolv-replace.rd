#@if (version >= "1.6.0")
[[m:BasicSocket#send]]、
[[m:IPSocket.getaddress]]、
[[m:TCPSocket.new]]、
[[m:TCPSocket.open]]、
[[m:UDPSocket#connect]]、
[[m:UDPSocket#send]]
の名前解決に [[lib:resolv]] ライブラリを使うようにメソッドを書き換えます。

[[unknown:執筆者募集]] 一応書いただけなので適当に書き直してください。
#@end
