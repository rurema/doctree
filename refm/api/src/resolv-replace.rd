(({((<BasicSocket>))#send}))、
(({((<IPSocket>)).getaddress}))、
(({((<TCPSocket>)).new}))、
(({((<TCPSocket>)).open}))、
(({((<UDPSocket>))#connect}))、
(({((<UDPSocket>))#send}))
の名前解決に ((<resolv>)) ライブラリを使うようにメソッドを書き換える。

((<執筆者募集>)) 一応書いただけなので適当に書き直してください。
