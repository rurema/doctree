= module WEBrick::HTTPAuth::Authenticator

== Instance Methods

--- logger -> object

ロガーオブジェクトを返します。

--- realm -> String

レルムを返します。

--- userdb
#@todo

== Constants

--- RequestField -> String

リクエストフィールド

--- ResponseField -> String

レスポンスフィールド

--- ResponseInfoField -> String
#@todo

--- AuthException -> Class

認証失敗時に発生する例外クラスです。

--- AuthScheme -> nil
#@todo

派生クラスで上書きしなければなりません。
