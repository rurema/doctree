# $Id: tklabel.rd,v 1.13 2001/03/18 12:39:30 arai Exp $
# Class TkLabel is defined in tk.rb
=begin
= TkLabel

ラベルウィジェットのクラスです。
ラベルウィジェットはラベル(文字列)を表示するためのウィジェットです。

  require "tk"
  TkLabel.new {
    text "label"
    pack
  }
  Tk.mainloop

== スーパークラス:

((<TkWindow>))

== メソッド:

--- TkLabel#textvariable(var)
#@todo
    ((<TkVariable>))のインスタンス((|var|))の内容をラベルにします。
    ((|var|))の内容が変われば自動的に表示されるラベルも変化します。

== 標準オプション:

((<TkObject#anchor>))

((<TkObject#background>))

((<TkObject#bitmap>))

((<TkObject#borderwidth>))

((<TkObject#cursor>))

((<TkObject#font>))

((<TkObject#foreground>))

((<TkObject#highlightbackground>))

((<TkObject#highlightcolor>))

((<TkObject#highlightthickness>))

((<TkObject#image>))

((<TkObject#justify>))

((<TkObject#padx>))

((<TkObject#pady>))

((<TkObject#relief>))

((<TkObject#takefocus>))

((<TkObject#text>))

### ((<TkObject#textvariable>))

((<TkObject#underline>))

((<TkObject#wraplength>))

== オプション:

--- TkLabel#height
#@todo

--- TkLabel#width
#@todo

=end
