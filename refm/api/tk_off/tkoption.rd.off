# $Id: tkoption.rd,v 1.9 2000/01/22 08:52:56 arai Exp $
# Module TkOption is defined in tk.rb
=begin
= TkOption

((<オプションデータベース>))をアクセスするためのモジュールです。

  require "tk"
  TkLabel.new(nil, "text"=>"label1").pack
  TkOption.add("*Label.background", "white")
  TkLabel.new(nil, "text"=>"label2").pack
  TkOption.clear
  TkLabel.new(nil, "text"=>"label3").pack
  Tk.mainloop

== インクルードしているモジュール:

((<Tk>))

== extendしているモジュール:

((<Tk>))

== モジュール関数:

--- TkOption.add(pat, value, pri=None)
#@todo
    新しいオプションをデータベースに追加します。
    データベースの変更結果は、その前に作成したオブジェクトには影響しません。
    ((|pri|))を指定することで、オプションの優先度を0から100までの数値に変
    更することができます。

--- TkOption.clear
#@todo
    オプションデータベースに対して行った変更をデフォルトに戻します。

--- TkOption.get(win, name, klass)
#@todo
    オプションデータベースから値を取得します。

--- TkOption.readfile(file, pri=None)
#@todo
    (({.Xdefaults})) などのリソースファイルを読み込みます。

=end
